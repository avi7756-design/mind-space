# ===== Smart Shopping Assistant — environment placeholders =====
# All variables are optional today (the app runs fully on mock data).
# They become required only when a real backend / integrations are wired in.

# Base URL of the future supplier-aggregation backend API
VITE_API_BASE_URL=

# API key for the future price/market-data provider
VITE_MARKET_DATA_API_KEY=

# Alert delivery integrations (used by the future alerts backend)
VITE_TELEGRAM_BOT_TOKEN=
VITE_EMAIL_SERVICE_API_KEY=
VITE_DEFAULT_WEBHOOK_URL=
